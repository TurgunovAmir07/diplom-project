import { Button } from "@mui/material";
import "../../index.css";

const CustomButton = () => {
  return (
    <Button
      fullWidth
      className="Button"
      style={{
        color: "#fff",
        background: "#003465",
      }}
    >
      text
    </Button>
  );
};

export default CustomButton;
